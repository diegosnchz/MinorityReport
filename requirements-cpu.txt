fastapi==0.109.0
uvicorn==0.27.0
# neo4j==5.16.0 # Removed specific version constraint if causing issues, but keeping for now
neo4j>=5.16.0
torch==2.2.0
torch-geometric
pydantic==2.6.0
python-dotenv
numpy
pandas
faker

# Evasion Protocol (CPU Adapters)
xgboost # Standard CPU version
shap
panel
bokeh
holoviews
optuna
hvplot

# HPC & MLOps Evolution (CPU Fallbacks)
pyarrow
numba
zarr
xarray
kfp
dask
fastparquet
httpx
scikit-learn # Replacement for cuml
